            );\n            // REMOVIDO: leitura local do token apenas para debug\n\n            // Verificar permissão de acesso ao cadastramento de dispositivos\n            final permissaoUrl = Uri.parse(\n              ApiConfig.getEndpoint('condominio', 'param'),\n            );\n            final permissaoPayload = {\n              \"param_txt\": \"param_portaria_acessa_pessoas\",\n            };\n            try {\n              final permissaoResponse = await http.post(\n                permissaoUrl,\n                headers: {\n                  'Content-Type': 'application/json',\n                  'Authorization': 'Bearer $tokenSessao',\n                },\n                body: jsonEncode(permissaoPayload),\n              );\n\n              if (permissaoResponse.statusCode == 200) {\n                final permissaoData = jsonDecode(permissaoResponse.body);\n                // Verificar se a resposta contém o campo valor_txt\n                String? valorTxt;\n                if (permissaoData is Map) {\n                  if (permissaoData['data'] is Map) {\n                    valorTxt = permissaoData['data']['valor_txt']?.toString();\n                  } else if (permissaoData['valor_txt'] != null) {\n                    valorTxt = permissaoData['valor_txt']?.toString();\n                  }\n                }\n\n                // Se valor_txt for 'N', bloquear o acesso\n                if (valorTxt != null && valorTxt.toUpperCase() == 'N') {\n                  // Limpar dados de sessão\n                  await prefs.remove('tokensessao_txt');\n                  await prefs.remove('usuario_id');\n                  await prefs.remove('condominio_id');\n                  await prefs.remove('usuario_nome');\n\n                  setState(() {\n                    _loading = false;\n                    _error =\n                        'Você não tem permissão para acessar o cadastramento de dispositivos. Entre em contato com o administrador.';\n                  });\n                  return;\n                }\n                // Se valor_txt for 'S' ou não existir, permitir o acesso\n              }\n              // Se a API de permissão falhar, permitir o acesso (fallback)\n            } catch (e) {\n              // Se houver erro na verificação de permissão, permitir o acesso (fallback)\n              print('Erro ao verificar permissão: $e');\n            }\n\n            // Verificar se há observação de turno pendente\n
